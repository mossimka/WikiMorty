import { FormatEpisodePipe } from './format-episode.pipe';

describe('FormatEpisodePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEpisodePipe();
    expect(pipe).toBeTruthy();
  });
});
